# Raw Data

This folder originally contained the raw Olist e-commerce CSV datasets downloaded from the public source.

The raw files are not included in this repository due to size limitations.

Raw datasets were used as the initial input for data cleaning and transformation using Python (Pandas & NumPy).